#!/bin/sh
# Toggle whether oneko is running or not.

oneko_cmd="oneko -tofocus -speed 32"

if [ "$(pgrep -fc "$oneko_cmd")" -gt 0 ]; then
    echo "killing oneko..."
    killall -9 oneko
    xsetroot -cursor_name left_ptr

else
    echo "starting oneko!"
    $oneko_cmd &
fi
